# 每日学习问题记录

> 2026-06-11 的 24 个问题，全部围绕 nanoGPT `model.py` 精读。

---

## 2026-06-11 (四) — 24 问全部回答

### 基础概念（Q1-Q6, Q12, Q21）

**Q1 — 把离散的整数变成连续的向量无法理解**
> 这就是 Embedding 层做的事。token ID（比如 42）本身只是个编号，没有任何语义。Embedding 是一张表，第 42 行存着一个向量（比如 768 个浮点数），查表取出这个向量，它就"代表"了这个 token 的语义。之所以"连续"，是因为这些浮点数可以在空间中计算距离——语义相近的词，向量也相近。

**Q3 — wte、wpe 等都是啥**
> | 缩写 | 全称 | 形状 | 含义 |
> |------|------|------|------|
> | wte | Word Token Embedding | (vocab_size, n_embd) | 查表：token ID → 向量 |
> | wpe | Word Position Embedding | (block_size, n_embd) | 查表：位置 0,1,2... → 向量 |
> | c_attn | Combined Attention | (n_embd, 3×n_embd) | Q、K、V 拼在一起的投影矩阵 |
> | c_proj | Combined Projection | (n_embd, n_embd) | Attention 输出后的投影 |
> | c_fc | Combined Fully-Connected | (n_embd, 4×n_embd) | MLP 第一层（膨胀到 4 倍） |
> | c_proj(mlp) | | (4×n_embd, n_embd) | MLP 第二层（压缩回来） |
> | ln_f | LayerNorm Final | (n_embd,) | 最后的 LayerNorm |

**Q12 — C维度和T维度是啥**
> | 符号 | 含义 | nanoGPT 默认值 |
> |------|------|:--:|
> | T | 序列长度（Time / Tokens），一句话有多少个 token | block_size=256 |
> | C | 嵌入维度（Channels），每个 token 用多长的向量表示 | n_embd=384 |
> | B | 批次大小（Batch），一次训练塞几句 | batch_size=64 |
> | nh | 注意力头数（number of Heads） | n_head=6 |
> | hs | 每头维度（Head Size）= C / nh | 384/6=64 |
> | V | 词表大小（Vocabulary） | vocab_size=50304 |

**Q21 — loss 和 logits 分别是啥**
> | 术语 | 是什么 | 形状 |
> |------|--------|------|
> | logits | 模型最后一层输出的原始分数（未经 softmax 的"概率"），可以是负数 | (B,T,V) |
> | loss | 把 logits 和正确答案（targets）比较，用交叉熵算出的一个数——越小越好 | 标量 |
>
> `logits` 告诉你每个位置每个 token 的"倾向"；`loss` 告诉你整体偏了多远。

**Q13 — FP16 是啥**
> 浮点数的一种，只占 16 位（2 字节），普通 FP32 占 32 位（4 字节）。好处：省显存、算得快。代价：精度低，太小或太大的数会溢出。所以训练时用 AMP（自动混合精度），大部分用 FP16 算，关键地方用 FP32 保精度。

**Q24 — weight decay 是啥**
> 优化器在更新参数时，除了往梯度方向走，还额外把参数的绝对值往 0 拉一点。效果是防止某些参数涨得太大导致过拟合。数学上等价于给 loss 加了一个 L2 惩罚项。

---

### Embedding 相关（Q2）

**Q2 — 两个 embedding 直接逐元素相加，原来的信息还存在吗**
> 存不存取决于你怎么看。"逐元素加"不是把信息抹掉，而是把两个来源的信息**融合**到一起。比如位置 3 的 token "cat"：
> ```
> token_emb("cat")  = [0.1, 0.2, -0.3, 0.5]
> pos_emb(3)        = [0.0, -0.1, 0.2, 0.0]
> 相加结果 x        = [0.1, 0.1, -0.1, 0.5]
> ```
> 第一维：0.1 里既包含"cat 的语义贡献"也包含"位置 3 的位置贡献"——分不开了，但神经网络不在乎分开，它只在乎后续的线性变换能从相加结果里提取出有用的模式。实验证明 Transformer 能自动学会区分语义信息和位置信息。

---

### Attention 相关（Q7, Q8, Q9, Q14）

**Q7 — softmax 数学原理是啥**
> $$softmax(z_i) = \frac{e^{z_i}}{\sum_j e^{z_j}}$$
>
> 三件事：① 把任意实数变成正数（$e^x > 0$）；② 所有输出加起来 = 1（变成概率分布）；③ 大的值被放大，小的值被压得更小。这就实现了"重点关注大的，忽略小的"。

**Q9 — nh 个头是啥，啥是"头"**
> 一个"头"= 一套独立的 Q/K/V 投影，从自己的角度去看输入。比如 nh=6：把 384 维切成 6 个 64 维的子空间，每个子空间独立做 Attention。6 个头可能一个关注"动词-宾语"关系，一个关注"形容词-名词"关系，一个关注位置邻近关系。最后把所有头拼回来。

**Q8 — 加权融合那一步是干啥**
> Attention 的核心目的：让每个 token 的表示**融入上下文信息**。"sat" 本来只是一个词向量，做完 Attention 后变成 12%×V_The + 65%×V_cat + 23%×V_sat——意思是"'sat' 这个词在'The cat sat'这句话里，受到'cat'最强烈的影响"。不加这步的话，每个词只知道自己，不知道上下文。

**Q14 — 训练时随机切断一些注意力连接是啥意思**
> 这是 Attention Dropout。训练时随机把 attention weights 的一些位置置零（神经元"失活"），每个 batch 随机关不同的位置。效果：模型不能依赖某一条固定的注意力路径，必须学会多条路径都 work——这等于强迫模型学得更鲁棒。推理时不关。

---

### MLP 相关（Q17）

**Q17 — 先膨胀再压缩是想干啥**
> MLP 的设计：`C → 4C → C`（比如 384 → 1536 → 384）。膨胀到 4 倍给模型足够的"思考空间"做非线性变换，再压缩回来方便和下一层对接。这是 Transformer 论文的标准设计，4 倍是实验调出来的。

---

### Dropout 相关（Q20）

**Q20 — dropout 具体干了啥，三种 dropout 的区别**
> | 类型 | 作用位置 | 随机丢弃什么 |
> |------|---------|------------|
> | Attention Dropout | attention weights 矩阵里 | 随机某些注意力连接 |
> | Residual Dropout | 残差加完之后 | 随机某些维度的残差贡献 |
> | Embedding Dropout | wte+wpe 之后 | 随机某些维度的嵌入 |
>
> 共同原理：训练时随机让一部分神经元"闭嘴"→ 模型不能依赖单个神经元 → 测试时不关，所有神经元一起干活 → 等于隐式地集成了一大堆子模型。

---

### 残差（Q19）

**Q19 — 残差是啥**
> ```
> out = x + Attention(LayerNorm(x))   ← 这就是残差连接
> ```
> 不加残差：网络深了梯度消失，训不动。加了残差：梯度可以通过"x"这条高速路直接传回浅层，深层网络也能训。直观理解：Attention/MLP 只需要学"和输入**不一样**的部分"（残差），不需要把整个变换重新学一遍。

---

### PyTorch 张量操作（Q15, Q18）

**Q15 — transpose 改的是 stride，不是实际内存排布。view 要求连续内存。没懂**
> 一个 `[B,T,C]` 张量，物理内存里就是 B×T×C 个数挨着排。`transpose(1,2)` 后逻辑形状变成 `[B,C,T]`，但物理内存没变——PyTorch 只改了 stride（每一步跨几个元素），不拷贝数据。`view()` 要求物理内存必须是连续的才能"重新解读"形状；如果之前 transpose 过，内存不连续了，view 就报错，需要先 `.contiguous()` 拷贝一份。

**Q18 — stride 和 view 有啥区别**
> | | stride | view |
> |------|--------|------|
> | 是啥 | Tensor 的属性：每一维跨几个元素 | 方法：改变 Tensor 形状 |
> | 改内存吗 | 不改 | 不改（只改解读方式） |
> | 举例 | shape(2,3) stride(3,1) 意思：第0维每步跨3个元素，第1维跨1个 | `x.view(2,3,4)` 把扁的变成三维 |

---

### 推理/采样相关（Q22, Q23）

**Q22 — 推理模式和训练模式的区别**
> | | model.train() | model.eval() |
> |------|:--:|:--:|
> | Dropout | 生效（随机丢） | 关闭 |
> | BatchNorm | 用当前 batch 统计 | 用全局统计 |
> | 梯度 | 计算 | 不计算（torch.no_grad） |
> | 目的 | 学习 | 只前向，不更新参数 |

**Q23 — Temperature 缩放是啥原理**
> 采样前把 logits 除以 temperature：
> ```
> probs = softmax(logits / T)
> ```
> | T | 效果 | 场景 |
> |:--:|------|------|
> | T=1 | 原始分布，不改 | 默认 |
> | T<1（如 0.7） | 放大高分 token 差距 → 输出更保守、重复 | 需要稳定输出 |
> | T>1（如 1.2） | 缩小差距 → 输出更随机、有创意 | 写诗、脑洞 |

---

### 架构理解（Q4, Q5, Q6, Q10, Q11, Q16）

**Q4 — shape 又是啥**
> shape = 张量在每个维度上的大小。`x.shape = (B=64, T=256, C=384)` 意思是：64 句话，每句 256 个 token，每个 token 用 384 个浮点数表示。写代码时 `print(x.shape)` 是最常用的调试手段。

**Q16 — 信息重组能力是咋表现出来的**
> 经过 Attention 后，每个 token 的向量不再只包含自己，还包含了它"注意到"的其他 token 的信息。"sat" 的新向量里融入了 "cat" 的信息——之后 MLP 再处理这个向量时，就能基于"sat 和 cat 有关"这个事实做推理。这就是信息重组。

**Q5, Q6, Q10, Q11 — 组织方式建议**
> 同意。源码注释应该：① 先写"这一步在做什么"（高层）；② 再解释关键参数（底层）；③ 所有说明紧跟源码行，别另起章节。

---

## 2026-06-12 (五)

| # | 科目/方向 | 问题描述 | 状态 | 解决思路/答案 |
|---|----------|---------|------|-------------|
| 1 | | | | |

### 今日小结


---
*记录于 2026-06-11，全部 24 问已回答*
